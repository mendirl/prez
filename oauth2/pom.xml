<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.1.0 http://maven.apache.org/xsd/maven-4.1.0.xsd">
    <modelVersion>4.1.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>io.mendirl.demo</groupId>
    <artifactId>oauth2</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>oauth2</name>
    <description>Démonstration OAuth2 Client Credentials entre deux services REST</description>

    <subprojects>
        <subproject>resource-server</subproject>
        <subproject>client-service</subproject>
        <subproject>frontend-htmx-service</subproject>
        <subproject>frontend-vue-service</subproject>
    </subprojects>

    <properties>
        <java.version>25</java.version>
    </properties>

    <profiles>
        <profile>
            <id>nullability</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>am.ik.maven</groupId>
                        <artifactId>nullability-maven-plugin</artifactId>
                        <version>0.4.3</version>
                        <extensions>true</extensions>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>configure</goal>
                                    <goal>generate-package-info</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>native</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>build-image</id>
                                <goals>
                                    <goal>compile-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
